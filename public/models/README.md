# Blender Export Instructions

- Export as GLTF 2.0 (`.glb` preferred for binary bundling)
- Include normals, tangents, and UVs; embed textures when possible
- Keep file sizes reasonable for fast iteration
- Place exported files here, e.g. `model.glb`
